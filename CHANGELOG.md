# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与
[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **RAPL 功耗改为多域采集**：枚举 `powercap` 下所有域（psys 平台 / package-0 封装 / core 核心 /
  uncore 核显与内存控制器 / dram 内存），逐域算差分瓦数；主值优先 `psys`（最接近整机），
  性能页的功耗卡显示主值瓦数 + 其余域明细（对应参考图的功耗构成分解）。
- 新增 `deploy/dashboard.service`（systemd 单元，刻意不以 root 运行，用 `ExecStartPre`
  单独放开 RAPL 读权限）与 `deploy/60-dashboard-rapl.rules`（udev 备选方案），
  README 增加「部署到 systemd」章节。
- 采集层新增 `DASHBOARD_RAPL_DIR` 环境变量，可把 RAPL 根目录指到夹具目录，便于测试。

### 新增

- **「进程」页**：全量进程表（本机约 300 行）+ 搜索 + 点列头排序 + 点行展开。
  - CPU 按逻辑核心数归一化到 0–100%，与概览页口径一致；
  - 锁定排序：刷新只更新数值不重排行，换列或再点列头才重排，新进程追加到末尾并高亮 6 秒；
  - 行展开显示命令行、启动时间（含已运行时长）、状态、线程数、用户、所属容器；
  - 容器归属来自 `/proc/<pid>/cgroup`（兼容 docker / containerd / podman 写法）+ `docker ps` 的 ID→名字映射；
  - 新增 `/api/processes`；进程的静态字段（用户/命令行/容器 ID）按 pid 缓存，概览快照只保留前 6 条，
    避免每次轮询拖着整张表。

### 新增

- **「网络与磁盘」页**（版式参照 `docs/reference/03-network-disk.png`）：
  上方大卡是网速（下载/上传两个大数字 + 2 分钟双线曲线），下方三张卡是连接 / 网卡 / 磁盘，
  再往下是磁盘读写曲线（读 + 写）与挂载点明细、当前连接的对端列表。
  - 连接卡：本机 IP、网关（`/proc/net/route`）、上网方式、本地代理（探测常见代理端口）、
    外网延迟（TCP 握手耗时，目标可配置）、网络连接数；
  - 网卡卡：链路状态、速率、双工、MTU、MAC、IPv4/IPv6、累计收发、丢包；
  - 磁盘卡：设备、型号（`udevadm` 完整名称）、容量、SSD/HDD、挂载点、累计读写；
  - 挂载点列表过滤 tmpfs/overlay 与 snap 的 `loop*`/`squashfs`（本机 31 个挂载里 28 个是 snap 噪声）；
  - 新增 `/api/network` 与曲线键 `disk_read` / `disk_write`；延迟探测放在独立线程，
    连接超时不会拖慢 1 秒采样。

### 变更

- 「网络与磁盘」页改为一屏布局：网速卡高度减半（29vh），磁盘读写曲线紧跟在网速卡下面，
  移除「当前连接」卡（连接概况仍在「连接」卡头注），下方三张卡吃掉剩余高度；
  桌面端整页不再滚动（内容超出时卡片内部滚动），手机端仍走整页滚动。

- **「性能与电源」页按参考图版式重做**：上半部分一张大卡（左列大数字 + 按物理核心分组的占用条 +
  GPU 频率，右侧 2 分钟大曲线 + 图例 + 「最近 2 分钟」），下半部分三张并排小卡
  （温度 / 功耗与风扇 / 内存构成），全部采用「大数字 + 小单位 + 细条/行式明细」的视觉语言。
- 新增 `cpu_max` 曲线键（每核占用最大值），大卡曲线画「总占用 + 最高单核」两条线。
- 内存图例改为两列，并单列「交换」。

### 修复

- **RAPL 域的可靠性校正**（实测发现）：
  - 同一计数器常同时暴露 `intel-rapl:*` 与 `intel-rapl-mmio:*` 两个接口，名字相同数值一致，
    原先按名字互相覆盖，现在按域名去重并优先 MSR；
  - `psys`（平台功耗）在本机未正确实现：满载时封装 12.5 W、psys 只报 3.6 W。
    主值改为「psys 不小于 package-0 时才用 psys，否则用 package-0」，副标题写明来源；
    不可信的 psys 在明细里标「未实现」；
  - 不做「计数器不前进＝未实现」的推断：空闲域（如 dram）本来就是 0.00 W，
    这样标注会随负载来回跳，属于假话；只保留「平台功耗不可能小于封装」这条有物理依据的校验。
- 权限相关的两处：
  - 首次权限失败后不再永久缓存：冷却 60 秒自动重试，放开权限后不必重启服务；
  - 部分域读不到时不再整体返回不可用，改为跳过并在载荷里带 `skipped`，页面标「N 项无权限」。
- udev 规则：`trigger` 需带 `--action=add`；core/uncore/dram 子域不是 udev 设备，
  规则里连同子目录一起 chmod（否则功耗构成会缺项）。

- 修复后台回前台后曲线出现一条横穿画面的直线：切后台时轮询暂停，回前台时
  `visibilitychange` 补拉与 `setInterval` 的 tick 可能同时在飞，两者用同一个旧 `since`
  拉到同一批点并各追加一次，曲线里出现时间倒退的重复点。现在页面轮询有并发闸门
  （上一次没回来就跳过本次触发），图表库也会丢弃重复/回退的时间戳。
- 图表在时间断层处改为断线：服务重启、机器休眠或轮询长时间中断后，不再把断开的两段
  连成一条直线；断线阈值跟随服务端采样间隔（接口新增 `interval` 字段）。

### 新增

- 前端重构为 hash 路由单页应用：`app.js` 只保留核心（路由/徽章轮询/公共工具），
  页面拆分为 `static/pages/` 下的按需加载模块；未上线的页签显示「建设中」。
- 新页面「性能与电源」：CPU 总占用与每核占用曲线（含每线程占用条与实时频率）、
  5 路温度通道（封装/核心/PCH/机身温区）、CPU 风扇转速曲线、核显当前/最大频率、
  内存构成（应用/缓冲/缓存/空闲堆叠条 + 交换）、电池状态（电量/接通状态/循环次数/放电功率）、
  负载 1/5/15；采不到的通道照旧显示「不可用 + 原因」。
- 采集层新增每核占用与频率、hwmon 风扇、Intel 核显频率、电池（sysfs power_supply）、
  内存细分（缓冲/缓存/共享）；`/api/performance` 接口与 `/api/series?keys=` 按需取序列
  （未知键返回 400），新增序列键 `cpu0..n` / `fan_cpu` / `gpu_mhz` / `temp_acpi`。

### 移动端适配（上一批次）

- ≤900px 时侧边栏折叠为吸顶顶部栏（页签横向滑动、暂停按钮上移顶栏），
  指标行改为「数值在上、曲线在下」，下半部分卡片单列；≤560px 进一步收紧字号与间距，
  并修正 iOS 不支持的 `background-attachment: fixed`。
- 页面切到后台或锁屏时自动暂停轮询，回到前台立即刷新一次，省电省流量；
  首次渲染不受该策略影响，后台标签页打开不会白屏。

### 计划中

- 剩余页面：进程、网络与磁盘、服务（含可配置 TCP 探测）、设备、传感器。
- 口令 / Token 鉴权：排在其他功能完善之后再做（当前定位是内网测试阶段，见 `SECURITY.md`）。
- systemd 用户级（`--user`）服务示例（系统级单元已在 `deploy/` 提供）。

## [0.1.0] - 2026-09-22

首个版本：概览页可用。

### 新增

- 零依赖 HTTP 服务（Python 标准库 `http.server`），监听 `0.0.0.0:8282`。
- 指标采集层：CPU、内存、整机功耗（Intel RAPL）、网速、磁盘、温度、负载。
  任何一项采不到都返回「不可用 + 原因」，不影响其它指标。
- 增量曲线接口：`/api/series?since=<ts>` 只返回新增数据点。
- 时序环形缓冲：每秒采样，页面随时打开都有最近 120 秒的曲线。
- 深色看板前端：手写 SVG 折线图、渐变填充、随容器拉伸。
- 上半部分 5 条指标铺满全宽；下半部分为「此刻最忙的程序」与「本机服务状态」。
- 服务状态面板：Docker 容器（5 秒缓存）、SSH 与监听端口数、CPU 温度、系统负载。
- 网速默认只统计物理网卡，排除 `docker0`/`br-*`/`veth` 等虚拟接口。
- 启停脚本 `run.sh`（`setsid` 脱离会话，父进程退出不会带走服务）。
- 44 个 `unittest` 用例，覆盖采集、缓冲、接口契约与降级路径。
- GitHub Actions：多 Python 版本跑 ruff 与测试。

[Unreleased]: https://github.com/Xiaji-yu/dashboard/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/Xiaji-yu/dashboard/releases/tag/v0.1.0
