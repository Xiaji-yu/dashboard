# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与
[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 修复

- 修复后台回前台后曲线出现一条横穿画面的直线：切后台时轮询暂停，回前台时
  `visibilitychange` 补拉与 `setInterval` 的 tick 可能同时在飞，两者用同一个旧 `since`
  拉到同一批点并各追加一次，曲线里出现时间倒退的重复点。现在页面轮询有并发闸门
  （上一次没回来就跳过本次触发），图表库也会丢弃重复/回退的时间戳。
- 图表在时间断层处改为断线：服务重启、机器休眠或轮询长时间中断后，不再把断开的两段
  连成一条直线；断线阈值跟随服务端采样间隔（接口新增 `interval` 字段）。

### 新增

- 前端重构为 hash 路由单页应用：`app.js` 只保留核心（路由/徽章轮询/公共工具），
  页面拆分为 `static/pages/` 下的按需加载模块；未上线的页签显示「建设中」。
- 新页面「性能与电源」：CPU 总占用与每核占用曲线（含每线程占用条与实时频率）、
  5 路温度通道（封装/核心/PCH/机身温区）、CPU 风扇转速曲线、核显当前/最大频率、
  内存构成（应用/缓冲/缓存/空闲堆叠条 + 交换）、电池状态（电量/接通状态/循环次数/放电功率）、
  负载 1/5/15；采不到的通道照旧显示「不可用 + 原因」。
- 采集层新增每核占用与频率、hwmon 风扇、Intel 核显频率、电池（sysfs power_supply）、
  内存细分（缓冲/缓存/共享）；`/api/performance` 接口与 `/api/series?keys=` 按需取序列
  （未知键返回 400），新增序列键 `cpu0..n` / `fan_cpu` / `gpu_mhz` / `temp_acpi`。

### 移动端适配（上一批次）

- ≤900px 时侧边栏折叠为吸顶顶部栏（页签横向滑动、暂停按钮上移顶栏），
  指标行改为「数值在上、曲线在下」，下半部分卡片单列；≤560px 进一步收紧字号与间距，
  并修正 iOS 不支持的 `background-attachment: fixed`。
- 页面切到后台或锁屏时自动暂停轮询，回到前台立即刷新一次，省电省流量；
  首次渲染不受该策略影响，后台标签页打开不会白屏。

### 计划中

- 概览页之外的页面：性能与电源、进程、网络与磁盘、服务、设备、传感器。
- 可选的口令/Token 鉴权，便于把看板安全地暴露到内网之外。
- systemd 用户服务示例。

## [0.1.0] - 2026-09-22

首个版本：概览页可用。

### 新增

- 零依赖 HTTP 服务（Python 标准库 `http.server`），监听 `0.0.0.0:8282`。
- 指标采集层：CPU、内存、整机功耗（Intel RAPL）、网速、磁盘、温度、负载。
  任何一项采不到都返回「不可用 + 原因」，不影响其它指标。
- 增量曲线接口：`/api/series?since=<ts>` 只返回新增数据点。
- 时序环形缓冲：每秒采样，页面随时打开都有最近 120 秒的曲线。
- 深色看板前端：手写 SVG 折线图、渐变填充、随容器拉伸。
- 上半部分 5 条指标铺满全宽；下半部分为「此刻最忙的程序」与「本机服务状态」。
- 服务状态面板：Docker 容器（5 秒缓存）、SSH 与监听端口数、CPU 温度、系统负载。
- 网速默认只统计物理网卡，排除 `docker0`/`br-*`/`veth` 等虚拟接口。
- 启停脚本 `run.sh`（`setsid` 脱离会话，父进程退出不会带走服务）。
- 44 个 `unittest` 用例，覆盖采集、缓冲、接口契约与降级路径。
- GitHub Actions：多 Python 版本跑 ruff 与测试。

[Unreleased]: https://github.com/Xiaji-yu/dashboard/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/Xiaji-yu/dashboard/releases/tag/v0.1.0
